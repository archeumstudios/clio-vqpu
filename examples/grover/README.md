# Grover search

The two-qubit workload marks `11` and succeeds exactly after one iteration. The three-qubit workload marks `111`; one iteration has analytic success probability 25/32 and is checked statistically with a fixed seed.
